
#include "Events.h"
#include <iostream>
#include <stdlib.h>
#include <fstream>
#include <string>

using namespace std;

class SaveGame {
private:
	vector <string> classType;
	vector <string> pathType;
	vector<float> stamina;
	vector<float> intellect;
	vector<float> strength;
	vector<float> willpower;
	vector<float> spirit;
	vector<string> weapon;
	vector<int> weaponDmg;
	vector<string> head;
	vector<int> headArmor;
	vector<string> body;
	vector<int> bodyArmor;
	vector<string> feet;
	vector<int> bodyArmor;
	//save up to 5 games
public:
	//save/load functions
};

int main() {
	do {
		
	} while (true);
}